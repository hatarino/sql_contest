SELECT
    DISTRICT_CODE AS CODE,
    DISTRICT_NAME AS NAME,
    TOTAL_AMT AS TOTAL
FROM POPULATION
WHERE DISTRICT_NAME LIKE '%東%' AND TOTAL_AMT >= 100000
ORDER BY TOTAL_AMT DESC, DISTRICT_CODE ASC;
